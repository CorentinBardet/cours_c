#include<stdio.h>
#include <stdbool.h>

float return_0(float c, float d){
	if (c == d){
		return 0;
	}
	else if (c < d){
		return -1;
	}
	else {
		return 1;
	}
}

int main(void){

	int a;
	int b;
	while (true){
		printf("Entrez deux nombres entiers: ");
		scanf("%d%d",&a,&b);
		int calc = return_0(a, b);

		if (calc == 0){
			printf("Les entiers sont égaux \n");
		}
		else {
			printf("Ils ne sont pas égaux \n" );
			printf("%d \n", calc);
		}
	}

	return 0;
}
